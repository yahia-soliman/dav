{
	"name": "bookodav",
	"version": "1.1.1",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"build": "wrangler build",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.11.1",
		"vitest": "^4.0.16",
		"wrangler": "^4.54.0"
	}
}